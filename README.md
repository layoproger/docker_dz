# Ветка для практики сборки image

# Команды которые я выполнял
# 1) это я смотрел, как закачать node js, тут чисто через инет, нас интересует именно Node.js версии 14.17.
# 2) Уже начал выполнять команды в корне виртуалки


# git clone https://github.com/AlariCode/docker-demo.git
# npm i -это устанавливает все зависимости

# сборка dockerfile

# FROM node:25
# WORKDIR /app
# ADD . .
# RUN npm install
# RUN npm run build api
# RUN RUN echo "проверка" > test.txt
# CMD ["node","./dist/apps/api/main.js"]
# docker build -f "путь до файла" -t "Название для своего контейнрера:версия (чаще всего latest)" .
# Точка в конце команды docker build указывает на контекст сборки. Это говорит Docker'у использовать текущую директорию (где выполняется команда) в качестве контекста, откуда копируются файлы в образ. Все пути в Dockerfile будут относительны этой директории.
