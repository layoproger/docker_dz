# Ветка для практики сборки image

# Команды которые я выполнял
# 1) это я смотрел, как закачать node js, тут чисто через инет, нас интересует именно Node.js версии 14.17.
# 2) Уже начал выполнять команды в корне виртуалки


# git clone https://github.com/AlariCode/docker-demo.git
# npm i -это устанавливает все зависимости

# сборка dockerfile

# FROM node:25
# WORKDIR /app
# ADD . .
# RUN npm install
# RUN npm run build api
# RUN RUN echo "проверка" > test.txt
# CMD ["node","./dist/apps/api/main.js"]
